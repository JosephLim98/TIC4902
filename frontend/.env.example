# Local dev (npm run dev) — browser talks to backend on host
VITE_API_BASE_URL=http://localhost:3000/api

# K8s image build uses VITE_API_BASE_URL=/api (see frontend/Dockerfile).
# API is proxied by gateway nginx -> svc/backend.
